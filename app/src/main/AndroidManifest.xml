<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.erp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".studentWork.attendancePage2"></activity>
        <activity android:name=".studentWork.attendancePage1" />
        <activity android:name=".studentWork.studentPage" />
        <activity android:name=".teacherWork.materialUpdate" />
        <activity android:name=".teacherWork.sample" />
        <activity android:name=".teacherWork.marksData" />
        <activity android:name=".teacherWork.marksUpdate" />
        <activity android:name=".teacherWork.AttendanceActivity" />
        <activity android:name=".teacherWork.selectSem" />
        <activity android:name=".teacherWork.teacherPage" />
        <activity android:name=".adminWork.addSubject" />
        <activity android:name=".adminWork.addStudent" />
        <activity android:name=".adminWork.addTeacher" />
        <activity android:name=".adminWork.adminPage" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".TeacherLogin" />
        <activity android:name=".StudentLogin" />
        <activity android:name=".addAdmin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>